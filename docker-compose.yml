version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: nexsori-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: nexsori
      POSTGRES_PASSWORD: nexsori_dev_password
      POSTGRES_DB: nexsori_learning
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nexsori']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: nexsori-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 5

  meilisearch:
    image: getmeili/meilisearch:v1.11
    container_name: nexsori-meilisearch
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: nexsori_search_dev_key
      MEILI_ENV: development
    ports:
      - '7700:7700'
    volumes:
      - meilisearch_data:/meili_data

volumes:
  postgres_data:
  redis_data:
  meilisearch_data:
